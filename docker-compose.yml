services:
  app:
    build: .
    image: labour-attendance:latest
    pull_policy: build
    container_name: labour-attendance
    ports:
      - "8000:8000"
    volumes:
      - ./models:/app/models:ro
      - ./data:/app/data
      - ./server/uploads:/app/server/uploads
    restart: unless-stopped
