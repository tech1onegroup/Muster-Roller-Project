# One Group UI

55 components. Samsung One UI structure + One Group branding. `shadcn/ui → Samsung styling → brand overlay`.

```bash
npm install
npx next dev                                    # One Group branded (default)
NEXT_PUBLIC_THEME=samsung-one-ui npx next dev   # Raw Samsung
```

See [ARCHITECTURE.md](ARCHITECTURE.md) for stack details. See [DESIGN.md](DESIGN.md) for brand guide index.
